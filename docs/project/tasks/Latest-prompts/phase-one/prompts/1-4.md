### 1-4: Stop swallowing database failures
```Read the root AGENTS.md first.

Task: Make database failures fail closed.

Target:
- `src/features/admin/infrastructure/persistence/postgres/index.ts`
- One focused regression test using the existing test convention

Find production code that catches PostgreSQL connection or query failures and returns empty rows, mock clients, in-memory values, or apparent success.

Change only this behavior:

- Real database failures must remain errors.
- Failed writes must not be reported as successful.
- Transactions must still roll back and release their client.
- Test-only fixtures may remain isolated inside test files.
- Existing public error behavior should be preserved where it is already defined.

Do not redesign repositories, change schema, or add a replacement fallback.

Add tests proving that a failed query and failed write are observable failures, not empty successful responses.

Verification:

pnpm exec tsc --noEmit
pnpm lint
pnpm exec <existing focused test command>
pnpm build
git diff --check

If a fallback is intentionally test-only, preserve it and explain its boundary in the final report.

```
