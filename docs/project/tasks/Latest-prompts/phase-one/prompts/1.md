###  1-1: Fix the Hero build blocker

```Read the root AGENTS.md first.

Task: Fix the existing JSX syntax error in `src/components/marketing/Hero.tsx`.

Evidence: Jules previously identified a `<motion.div>` whose closing tag is missing after a nested `</button>`. Inspect the complete component and correct only the JSX nesting.

Scope:
- `src/components/marketing/Hero.tsx`

Do not change styling, copy, component behavior, dependencies, or unrelated files. Do not reformat the entire file.

Verification:

pnpm exec tsc --noEmit
pnpm lint
pnpm build
git diff --check

If validation fails because of another pre-existing error, do not fix it in this task. Report the exact error and file.

Final report: changed file, root cause, commands run, exit status, and remaining unrelated failures.

```
