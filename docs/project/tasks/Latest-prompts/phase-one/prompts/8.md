### 1-8: Add the Milestone 1 stabilization tests
```Read the root AGENTS.md first.

Task: Add focused regression coverage for the completed Milestone 1 fixes.

Inspect the repository’s existing test convention before creating files.

Cover only these behaviors:

1. `Hero.tsx` compiles with valid JSX.
2. The dependency graph installs with the frozen pnpm lockfile.
3. Concurrent transactions do not share state.
4. PoolClients are released after commit and rollback.
5. Database failures do not become successful empty responses.
6. Forged tenant headers cannot establish authorization.
7. Audit creation and dashboard reads use the same canonical model.
8. The audit Inngest event has a registered consumer.

Use deterministic fixtures only inside tests. Do not add mock databases, fake production responses, or unrelated refactors.

Modify only:
- Existing test files
- New test files under the existing test convention
- `package.json` only if an existing test script is genuinely missing and adding it is necessary

Verification:

pnpm install --frozen-lockfile
pnpm exec tsc --noEmit
pnpm lint
pnpm build
pnpm exec drizzle-kit generate
pnpm test:acquisition
git diff --check

Do not claim coverage for checks that could not run. Report every command, exit status, skipped check, and remaining limitation.

```
