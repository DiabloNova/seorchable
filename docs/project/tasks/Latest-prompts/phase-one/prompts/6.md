### 1-6: Align audit persistence and dashboard reads
```Read the root AGENTS.md first.

Task: Fix the mismatch between audit creation and audit retrieval.

Inspect only:

- `src/app/actions/audit.ts`
- `src/app/actions/dashboard.ts`
- `src/services/dashboard-home/index.ts`
- Audit detail page data access
- Imported database schema files
- Existing audit migrations
- Relevant focused tests

Before editing, identify from repository evidence:

- Canonical audit table
- Canonical columns
- Existing repository or query boundary
- Tenant boundary
- Authorization boundary
- Valid audit statuses

Make audit creation, dashboard summaries, and audit detail reads use the same verified persistence model.

Do not invent an `audits` table, columns, repository methods, migration, or status values. Do not modify migrations unless the current repository evidence proves a correction is required.

If the canonical model cannot be established, stop with:

BLOCKED - INSUFFICIENT EVIDENCE

Add one focused regression test proving that an audit created through the supported action is visible through the dashboard read path.

Verification:

pnpm exec tsc --noEmit
pnpm lint
pnpm exec <existing focused test command>
pnpm build
git diff --check

```
