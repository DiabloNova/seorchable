### 1-5: Remove spoofable tenant headers
```Read the root AGENTS.md first.

Task: Prevent client-provided identity headers from establishing authorization.

Inspect:

- `src/services/auth/authorization.ts`
- `src/app/api/v1/dashboard/summary/route.ts`
- `src/app/api/v1/audit/premium/route.ts`
- `src/app/api/v1/audit/aeo-insight/route.ts`
- Existing authentication and authorization tests

Required behavior:

1. Browser requests derive user and workspace identity from the verified server session.
2. `x-user-id` and `x-tenant-id` cannot impersonate another user or workspace.
3. Existing role and workspace authorization behavior remains intact.
4. Unauthenticated requests return 401.
5. Do not invent a new API-key system.
6. Preserve an existing verified external API credential mechanism if one is actually present.

Add focused tests proving forged identity headers cannot access another workspace or bypass authentication.

Do not modify database schema or unrelated routes.

Verification:

pnpm exec tsc --noEmit
pnpm lint
pnpm exec <existing focused test command>
pnpm build
git diff --check

If external API authentication is not implemented, remove the unsafe header fallback and report that external API access remains blocked.

```
