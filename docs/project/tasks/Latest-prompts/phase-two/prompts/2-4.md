### 2-4: Implement real login and logout
```Read the root AGENTS.md first.

Task: Replace demo login behavior with real credential validation and secure logout.

Target only:

- src/app/actions/auth.ts
- src/services/auth/session.ts
- src/services/auth/authorization.ts
- src/components/AuthProvider.tsx
- src/app/[locale]/login/page.tsx
- One focused login test

Required behavior:

1. Login requires both email and password.
2. Credentials are checked against persisted user data.
3. Password verification uses the established password-hashing mechanism.
4. Unknown users and wrong passwords return the same generic error.
5. Inactive or unverified accounts follow the verified account policy.
6. Successful login creates a secure server session.
7. Logout revokes or deletes the server session.
8. No user identity is generated during login.
9. No password is logged, returned, or placed in client state.
10. Preserve existing locale-aware redirects.

Do not implement OAuth, password recovery, billing, or dashboard changes.

Verification:

pnpm exec tsc --noEmit
pnpm lint
pnpm build
git diff --check

Run the focused authentication test command from package.json or the repository test convention. Verify wrong password, unknown email, successful login, and logout invalidation.

```
