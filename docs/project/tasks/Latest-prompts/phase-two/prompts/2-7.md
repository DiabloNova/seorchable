### 2-7: Enforce server-side dashboard access and RBAC
```Read the root AGENTS.md first.

Task: Make dashboard access and workspace permissions server-enforced.

Target only:

- src/app/[locale]/dashboard/page.tsx
- src/app/[locale]/dashboard/layout.tsx
- src/components/ProtectedRoute.tsx
- src/services/auth/authorization.ts
- src/services/dashboard-home/index.ts
- src/app/api/v1/dashboard/summary/route.ts
- Focused authorization tests

Required behavior:

1. The dashboard server page rejects unauthenticated users before database reads.
2. Redirects preserve the active locale.
3. Client-side ProtectedRoute remains only a UX safeguard.
4. Server actions and route handlers validate the server session independently.
5. Workspace access is checked against persisted membership.
6. Viewer, workspace-admin, and super-admin permissions remain distinct.
7. Forged `x-user-id` and `x-tenant-id` headers cannot change identity.
8. Dashboard queries are tenant-scoped.
9. Unauthorized requests do not reveal tenant data.
10. Existing dashboard response contracts remain compatible.

Do not modify database schema or build a new authorization framework. If membership authorization cannot be proven from the repository, report BLOCKED.

Verification:

pnpm exec tsc --noEmit
pnpm lint
pnpm build
git diff --check

Start a disposable local server only for smoke testing, then stop it:

curl -i http://localhost:3000/fa/dashboard
curl -i http://localhost:3000/api/v1/dashboard/summary
curl -i -H 'x-user-id: forged-user' -H 'x-tenant-id: forged-tenant' http://localhost:3000/api/v1/dashboard/summary

Unauthenticated and forged requests must not return tenant data.

```
