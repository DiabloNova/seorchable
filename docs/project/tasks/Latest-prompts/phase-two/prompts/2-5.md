### 2-5: Implement email verification
```Read the root AGENTS.md first.

Task: Implement email verification for newly registered accounts using the repository’s verified email-provider contract.

Inspect first:

- src/app/[locale]/verify-email/*
- src/app/actions/*
- Existing email service code
- Existing token schema and migrations
- package.json
- .env.example

Required behavior:

1. Create a single-use, expiring verification token.
2. Store only a secure token hash where the existing architecture supports it.
3. Send the verification message through the existing configured provider.
4. Do not expose the raw token in logs.
5. Verify the token server-side.
6. Reject expired, reused, malformed, or unknown tokens.
7. Mark the account verified transactionally.
8. Make verification idempotent for an already verified account.
9. Do not activate fake verification when no provider exists.

If no canonical token model or email-provider contract exists, stop with:

BLOCKED - INSUFFICIENT EVIDENCE

Do not invent a provider or silently simulate delivery.

Verification:

pnpm exec tsc --noEmit
pnpm lint
pnpm build
pnpm exec drizzle-kit generate
git diff --check

Run focused verification tests for valid, expired, reused, and invalid tokens. Report provider-dependent checks that could not run.

```
