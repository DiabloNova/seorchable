### 2-2: Implement persisted users and sessions
```Read the root AGENTS.md first.

Task: Implement the persisted user and server-session foundation using the authentication contract established in the previous merged PR.

Target only the verified authentication schema, repository, session service, and focused tests.

Required behavior:

1. Users are persisted in PostgreSQL.
2. Passwords are stored only as secure password hashes.
3. Sessions are persisted or use the verified existing secure session mechanism.
4. Session cookies are HTTP-only, secure in production, SameSite-configured, and expiring.
5. Sessions can be revoked during logout.
6. User IDs and workspace IDs are persisted identifiers, never Math.random-generated values.
7. Database failures fail closed.
8. No mock or in-memory production persistence.

Do not implement registration UI, OAuth, billing, or email delivery in this task. Do not change unrelated schema.

If the previous contract is missing or ambiguous, stop with:

BLOCKED - INSUFFICIENT EVIDENCE

Verification:

pnpm exec tsc --noEmit
pnpm lint
pnpm exec drizzle-kit generate
pnpm build
git diff --check

Run the repository’s existing focused authentication test command if one exists. Report the exact command and exit status.

```
