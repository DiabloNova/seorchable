### 2-1: Establish the authentication data contract
```Read the root AGENTS.md first.

Task: Inspect and document the canonical authentication and workspace data model before implementation.

Inspect only:

- package.json
- database/schema/*
- database/migrations/*
- src/services/auth/*
- src/lib/auth.ts
- src/app/actions/auth.ts
- src/types/auth.ts
- src/app/[locale]/login/page.tsx
- src/app/[locale]/register/page.tsx
- Existing authentication tests

Identify:

- User table and columns
- Organization/workspace table and columns
- Membership or role model
- Session model
- Email-verification model
- Password-reset model
- Tenant propagation mechanism
- Authorization boundary
- Existing migration workflow

Do not change application behavior. Do not invent tables, columns, repositories, or migrations.

If the repository does not contain enough evidence for a real authentication model, report:

BLOCKED - INSUFFICIENT EVIDENCE

Include the exact files inspected and the missing evidence.

Verification:

pnpm exec tsc --noEmit
pnpm lint
git diff --check

Do not run a long-lived development server.

```
