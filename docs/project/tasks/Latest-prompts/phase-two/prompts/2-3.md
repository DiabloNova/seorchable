### 2-3: Implement registration and workspace onboarding
```Read the root AGENTS.md first.

Task: Connect registration to real persisted user and workspace onboarding.

Target only:

- src/app/[locale]/register/page.tsx
- src/app/actions/auth.ts
- Existing authentication repositories and services
- Verified authentication schema files
- One focused registration test

Required behavior:

1. Validate name, email, password, and workspace name on the server.
2. Normalize the email before lookup and persistence.
3. Reject duplicate accounts using the canonical repository.
4. Create the workspace from the submitted workspace name.
5. Create the user and workspace membership transactionally.
6. Assign the least privileged valid role.
7. Never accept a client-provided workspace ID.
8. Never create a random user or default `ws-default` identity.
9. Do not issue a fully active session before the verified account policy allows it.
10. Preserve Persian RTL and English LTR UI behavior.

Do not implement OAuth or password reset here. Do not invent schema names or columns. If the canonical membership model is unavailable, report BLOCKED instead of guessing.

Verification:

pnpm exec tsc --noEmit
pnpm lint
pnpm build
git diff --check

Run the focused registration test if available. Otherwise add no new test runner and report the missing test infrastructure.

```
